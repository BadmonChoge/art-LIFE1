name: Deploy ArtLife
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          mkdir -p /var/www/artlife
          cp -r ./* /var/www/artlife/
          echo "✅ Deployed! Visit: http://154.159.237.238/"
